{
    "private": true,
    "scripts": {
        "build:css": "tailwindcss -i resources/css/app.css -o dist/app.css --minify",
        "build:js": "terser resources/js/logscope.js -o dist/logscope.js -c -m",
        "build": "npm run build:css && npm run build:js"
    },
    "devDependencies": {
        "tailwindcss": "^3.4.0",
        "terser": "^5.46.0"
    }
}
